    1. create_df
# 테이블 합치기
tables에 추출한 테이블은 모두 첫 행이 column 헤더로 구성된다. 그대로 합치면 곤란해지기에 미리 헤더만 따로 저장해두고, 모든 테이블의 첫행을 삭제한다. 

# 컬럼 추출
인권과 평화, 대학생활세미나 등은 '확인사항'속성이 '교과목명'에 반영되어야 한다. 먼저 컬럼을 다루기 쉽도록 이름을 변경하고 확인사항 반영 후 최종적으로 필요한 컬럼만 남긴다. 
이런
""
특정 조건에 따라 특정 (행, 열)에 접근하여 값을 바꾸거나 출력하거나 하는 등은 벡터화를 통해 하는 것이 훨씬 빠릅니다. for문으로 행을 하나씩 반복하는 것은 크지 않은 데이터셋에 대해, 각 행별로 여러 가지 작업을 테스트해보는 경우에 주로 적합합니다. 
"" *출처-https://bio-info.tistory.com/149
라고 하길래 반복문은 사용 안하려고 했는데 
일부 수정하는거 잘안된다. 난 얼른 끝내야하니 일단 반복문으로 한다.

# 필요한 컬럼 남기기 
(인덱스가 숫자로 돼있어서 조건이 안맞음 첫행을 삭제하든가 
인덱스를 제대로 설정하기 )
테이블마다 컬럼 수가 달라서 각각 처리해줘야함. 
먼저 현재 첫행에 있는 속성들을 컬럼으로 설정한다. 이후 필요한 컬럼명만 남긴다.  
모든 테이블에 공통적으로 존재하는 컬럼들이기에 오류가 나지 않는다. 
[['이수\n구분','과목코드','교과목명','교수명','강의실/시간','확인사항']]

# 테이블 정리하면서 df 변수에 수정된 테이블을 계속 반영하고 있는데,
나는 필요없는 변수가 메모리에 저장되는게싫어서 그렇게 하지만
이 방법이 안 좋을수도 있을까?

# 공백제거 
현재 데이터 처리하거나 나중에 웹에서 강의 검색할 때 편리 

# roomntime 분리 중
이전까지는 주2회수업이 모두 room이 같았다. 이번에 다른 강의가 하나 나타났다. 따라서 이번에는 room1, room2로 나뉘어야 할것 같다. 일이 커진다. 
비슷하게 time이 다를수도 있기에 이것도 시간될때 처리해두어야 할듯. 아 망햇다
start1, end1, start2, end2가 되어야 한다. 

# 예외처리 
강의는 기본적으로 room1, date1, start, end가 있어야 하는데 
없는 강의가 있다. 이런건 그냥 처음부터 빼고 하는게 편하다. 
나중에 확정되면 그때 하나씩 추가하는것이 낫다. 

# 항상 까먹는 중복 제거 